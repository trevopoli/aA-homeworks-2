<h1>Add a New Band</h1>

<form action="<%= bands_url %>" method="post">
    <%= auth_token %>

    <label for="band_name">Band's Name: </label>
    <input type="text" name="band[name]" id="band_name">
    <br>
    <input type="submit" value="Add band">
</form>